# agent-internet-access
An agent with no network egress can still reach the internet, using only a storage surface it shares with an ordinary internal service. Every component passes the Rule of Two, but as a whole, the organisation does not.
